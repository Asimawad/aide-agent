#!/bin/bash

# # run the docker container
# docker run -d --name mlebench-container mlebench-image

# # run the docker container
# docker run -d --name mlebench-container mlebench-image

# # run the docker container
# docker run -d --name mlebench-container mlebench-image

# # run the docker container

echo "Brun script started..."